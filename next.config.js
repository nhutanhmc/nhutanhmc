/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  /* We can deploy as static HTML export if needed */
  // output: 'export',
  // distDir: 'build',
};

module.exports = nextConfig;
